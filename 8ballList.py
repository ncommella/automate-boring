import random

messages = ['It is so',
            'No',
            'Yes',
            'Why the hell not?',
            'The outlook is uncertain',
            'Ask again later']

print(messages[random.randint(0, len(messages)-1)])
